@using CSIDE.Web.Extensions

@(Value.ToLocalizedDisplayString(localizer, NullLabel))

@code {
    [Parameter, EditorRequired]
    public bool? Value { get; set; }
    [Parameter]
    public string? NullLabel{ get; set; }
}
