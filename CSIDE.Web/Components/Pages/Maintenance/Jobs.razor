@page "/Maintenance/Jobs"
@using CSIDE.Web.Components.Maintenance

<PageTitle>@localizer["Search Results Title"]</PageTitle>

<div>
    <Breadcrumb Items="NavItems"></Breadcrumb>
</div>

<h1>@localizer["Search Results Title"]</h1>

<JobsList AssignedToTeamId="@AssignedToTeamId"
            CompletedDateFrom="@CompletedDateFrom"
            CompletedDateTo="@CompletedDateTo"
            LogDateFrom="@LogDateFrom"
            LogDateTo="@LogDateTo"
            JobPriorityId="@JobPriorityId"
            JobStatusId="@JobStatusId"
            IsComplete="@IsComplete"
            ParishId="@ParishId"
            ParishIds="@ParishIds"
            RouteId="@RouteId">
</JobsList>    

